#include "main.h"
/**
 * print_numbers - prints the alphabet from 0 to 9
 * followed by new line
 * Return: none
 */
void print_most_numbers(void)
{
	int n;

	for (n = 0; i <= 9; i++)
	{
		if (i != 2 && i != 4)
		{
			_putchar(n);
		}
	}

	_putchar('\n');
}
