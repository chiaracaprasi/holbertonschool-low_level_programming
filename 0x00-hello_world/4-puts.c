#include <stdio.h>

/**
 * main - prints out the phrase as below
 * return - always 0 if successful 
 */
int main(void)
{
  
   puts("\"Programming is like building a multilingual puzzle");
   return(0);
   
 }
